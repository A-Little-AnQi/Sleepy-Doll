node_modules/
target/
ui-dist/
.sleepy-doll/
.retired/
.staging-*/
*.runtime.lock
# Design exploration assets. They are ~23 MB of unreferenced PNGs — roughly 90%
# of the repository pack — and neither the application nor the documentation
# loads them, so they live on disk rather than in version control.
/docs/design/
# User configuration belongs in <installation>/user/ and must never be committed.
# The rules below only stop accidental commits of local copies.
/user/
sleepy-doll*.config.json
!sleepy-doll.config.example.json
!sleepy-doll.mock.config.json
sleepy-doll.config.*.tmp
*.v1.backup.json
*.json.*.tmp
*.db
*.db-shm
*.db-wal
*.log
.sleepy-doll-dev.json
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
.idea/
.vscode/
*.swp
*~
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
coverage/
.DS_Store
Thumbs.db
BGI-Agent-Implementation-Plan.md:Zone.Identifier
